import { DefaultHeader } from './defaultHeader'

export default {
    title: 'SecureStore/DefaultHeader',
    component: DefaultHeader,
    tags: ['autodocs'],
    parameters: {
        layout: 'fullscreen'
    }
}

export const Primary = {
    args: {
        primary: true
    }
}

export const Secondary = {
    args: {
        
    }
}